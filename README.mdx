# Honeycomb Protocol Usage Demos

This repository contains a collection of demos that showcase how to use the Honeycomb Protocol. 

Each demo covers a different use-case of Honeycomb. The demos are written as tests that can be run using the `yarn test` command.

Please make sure to install all dependencies by running `yarn` when you clone this repo.